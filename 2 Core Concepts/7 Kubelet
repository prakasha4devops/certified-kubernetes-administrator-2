Kubelet:
=========

Register the node in the K8s cluster
Create PODS
Monitors Nodes and PODS
Kubeadm doesn't deploy Kubelet


ps -aux | grep Kubelet

    - Manually install kubelet in worker Nodes
    - 
